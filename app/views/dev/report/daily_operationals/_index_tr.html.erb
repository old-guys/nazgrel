<tr>
  <th scope="row"><%= record.id %></th>
  <td><%= time_tag record.report_date %></td>
  <% stat_fields.each {|field| %>
    <td><%= format_numberlize value: record.send(field) %></td>
  <% } %>
</tr>